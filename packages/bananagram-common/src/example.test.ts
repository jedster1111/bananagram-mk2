import { createPieces, numberOfBanangramTokens } from './example';

it('should do something', () => {
  const pieces = createPieces(numberOfBanangramTokens);
  console.log('Examples Pieces:', JSON.stringify(pieces));
});
